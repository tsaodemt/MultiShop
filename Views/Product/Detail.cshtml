@model Product
@{
    ViewBag.Title = Model.Category.Name + " | " + Model.Name;
    //Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

<div class="col-lg-12 header-bar">
     <div style="padding-top: 8px">@Model.Name</div>
</div>
<br />
<div class="col-lg-12">
    </div>
    <div class="col-lg-12">
        <img class="img-responsive" src="~/Content/img/Uploads/@Model.Image" />
    </div>
    <div class="col-lg-12 header-info">
       <div style="padding-top: 8px">THÔNG TIN SẢN PHẨM</div>
    </div>
<div style="padding-top: 20px">&nbsp;</div>
    <div class="col-lg-12">
        @Html.Raw(Model.Description)
    </div>
